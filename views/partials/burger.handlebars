<ul class="list-group list-group-flush">
    <li class="text-body list-group-item d-flex justify-content-between align-items-center">{{id}}. {{burger_name}}
        <button data-id="{{id}}" type="button" class="btn btn-primary consume-btn">🍔 Consume!</button>
    </li>
</ul>